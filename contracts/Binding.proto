syntax = "proto3";
package contract;

import "Error.proto";

option go_package = "./grpc/pb";

message Binding {
    string queue = 1;
    string exchange = 2;
    string routing_key = 3;
}

service BindingService {
    rpc Bind(Binding) returns (Error);
    rpc Unbind(Binding) returns (Error);
}